{
  "name": "ios-utility-app-web",
  "version": "1.0.0",
  "description": "Web UI layer for iOS Utility App — built with Vite + TypeScript Web Components",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.5.0",
    "@vitest/ui": "^1.5.0",
    "jsdom": "^24.0.0",
    "@vitest/coverage-v8": "^1.5.0"
  }
}
